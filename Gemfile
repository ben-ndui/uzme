source "https://rubygems.org"

gem "fastlane"
# Pin cocoapods so that `bundle exec pod install` uses a Ruby-3.2-compatible
# version, instead of the runner's system `pod` (lié au Ruby system, casse
# avec xcodeproj LoadError quand on installe Ruby 3.2 via setup-ruby).
gem "cocoapods"
